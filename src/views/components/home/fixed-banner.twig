{#
| المتغير    | النوع    | الوصف                                                               |
|-------------|---------|---------------------------------------------------------------------|
| url         | string  | رابط اللافتة                                                        |
| image.url   | string  | رابط صورة اللافتة                                                  |
| image.alt   | ?string | نص بديل للصورة (اختياري)                                         |
| position    | Int     | رقم ترتيب يبدأ من الصفر                                           |
#}
<section class="s-block s-block--fixed-banner wide-placeholder">
    <div class="container">
        <a href="{{ url }}" class="banner banner--fixed overflow-hidden" aria-label="Banner {{ image.alt }}">
            <img width="1200" height="300" src="{{ image.url }}" alt="{{ image.alt|trim ? image.alt : 'fixed banner' }}"/> <!-- صورة اللافتة -->
        </a>
    </div>
</section>
